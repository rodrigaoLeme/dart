import 'package:flutter/material.dart';

class VSpacer extends SizedBox {
  const VSpacer(double height, {Key? key}) : super(key: key, height: height);
}

class HSpacer extends SizedBox {
  const HSpacer(double width, {Key? key}) : super(key: key, width: width);
}
